/*
CS371p: Quiz #29 (5 pts)
*/

/* -----------------------------------------------------------------------
1. Describe a data structure and algorithm that will simulate the
   simultaneous evolution of the cells in the grid.
   [Life]
   (4 pts)

two grids of cell states
one pass through one grid updates the second grid
the grids are then switched

one grid of cell states
one grid of neighbor counts
one pass through the grid updates the counts
one pass through the grid updated the states
*/
